There are a number of useful scripts in the util/ subdir:

* install -- Installs the package as quickly as possible. Skips
  building the vignette.

* package -- Prepare the package for CRAN.

* rox -- Re-generate the documentation.

* test -- Run the test suite using the uninstalled tests against the
  installed package.
